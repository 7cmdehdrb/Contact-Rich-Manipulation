# 2026-07-10 Development Log

- Newton/rsl_rl/Torch import를 `conda run -n newton` 기준으로 확인했다.
- CUDA driver는 현재 세션에서 잡히지 않아 초기 검증은 CPU smoke test로 진행했다.
- `src/learning` 아래에 reaching 환경 기본 구조를 추가했다.
- Joint action 환경은 6D joint delta, Cartesian action 환경은 3D EEF delta로 분리했다.
- UR5e DH forward kinematics 기반 smoke backend를 먼저 두고, 추후 Newton URDF 로딩 backend를 붙일 수 있게 action/observation/reward 계약을 고정했다.
- UR description submodule을 `src/Universal_Robots_ROS2_Description`에 추가했고, UR5e xacro/config/mesh 경로 확인 helper를 추가했다.
- UR5e xacro를 flattened URDF로 자동 생성하는 스크립트를 추가했다.
- 기본 학습 backend를 실제 Newton Physics backend로 전환했다.
- Newton `ModelBuilder.add_urdf`, `ArticulationView`, `SolverMuJoCo` 기반으로 joint target 제어와 tool0 EEF 관측을 연결했다.
- Joint/Cartesian 환경 모두 실제 Newton backend에서 smoke rollout 및 rsl_rl 1 iteration 학습 시작을 확인했다.
